import React, { Component } from 'react'
import { connect } from 'react-redux'

export class CommodityAttr extends Component {
    render() {
        return (
            <div>
                2
            </div>
        )
    }
}

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(CommodityAttr)
